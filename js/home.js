document.addEventListener('DOMContentLoaded', function(){
      setInstrument();
      setDialog('prefectures');
      setDialog('numbersUnit');
      setDialog('dataUnit');
      setDialog('tax');
      setDialog('symbol');
});